// // src/store/modules/todoGetters.ts

// import { useTodoState } from './todoState';
// import { computed } from 'vue';

// export const useTodoGetters = (store = useTodoState()) => {
//   return {
//     completedTodosCount: computed(() => store.todos.filter((todo) => todo.completed).length),
//   };
// };
